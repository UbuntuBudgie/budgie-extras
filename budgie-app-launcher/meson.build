APPLICATIONS_DIR = join_paths(datadir, 'applications')
PLUGIN = 'budgie-app-launcher'


conf = configuration_data()
conf.set('PROJECT_NAME', meson.project_name())

message('Installing applet...')

subdir('src')

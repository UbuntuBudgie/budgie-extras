#gnome = import('gnome')

PLUGIN = 'budgie-workspace-stopwatch'

install_data(
    'BudgieWorkspaceStopwatch.plugin',
    'budgie-workspacestopwatch.py',
    install_dir: join_paths(PLUGINS_INSTALL_DIR, PLUGIN)
)

install_data(
    'budgie-wstopwatch-symbolic.svg',
    install_dir: PIXMAPS_DIR
)
